package bada_project_schronisk_KABM.SpringApp;

import org.apache.catalina.WebResourceRoot;
import org.junit.jupiter.api.Test;
import org.springframework.jdbc.core.JdbcTemplate;

import java.util.List;

import static org.junit.jupiter.api.Assertions.*;

class KlientDAOTest {

    @Test
    void list() {

//        KlientDAO klientDAO = new KlientDAO(new JdbcTemplate());
//        List<Klient> klienci = klientDAO.list();
//
//        assertNotNull(klienci, "Lista klientów nie powinna być null"); // Sprawdź, czy lista nie jest null
//
//        if (klienci.isEmpty()) {
//            System.out.println("Tabela Klienci jest pusta.");
//        } else {
//            System.out.println("Zawartość tabeli Klienci:");
//            for (Klient klient : klienci) {
//                System.out.println(klient); // Używamy toString() z klasy Klient
//            }
//        }
    }
}